import { MzSwitchDirective } from './switch.directive';

describe('MzSwitchDirective:view', () => {

  it('should create an instance', () => {
    const directive = new MzSwitchDirective(null, null);
    expect(directive).toBeTruthy();
  });
});

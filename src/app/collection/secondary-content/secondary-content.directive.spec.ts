import { SecondaryContentDirective } from './secondary-content.directive';

describe('SecondaryContentDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondaryContentDirective();
    expect(directive).toBeTruthy();
  });
});

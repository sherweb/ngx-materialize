import { MzTimepickerDirective } from './timepicker.directive';

describe('TimepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new MzTimepickerDirective();
    expect(directive).toBeTruthy();
  });
});

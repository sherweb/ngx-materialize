import { Routes } from '@angular/router';

import { FeatureDiscoveryComponent } from './feature-discovery.component';

export const ROUTES: Routes = [
  { path: '', component: FeatureDiscoveryComponent },
];
